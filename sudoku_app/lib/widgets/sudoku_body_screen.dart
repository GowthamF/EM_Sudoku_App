import 'package:flutter/material.dart';
import 'package:sudoku_app/sudoku_app.dart';

class SudokuBodyScreen extends StatelessWidget {
  const SudokuBodyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SudokuBody();
  }
}

import 'package:flutter/material.dart';
import 'package:sudoku_app/sudoku_app.dart';

class GameControlsScreen extends StatelessWidget {
  const GameControlsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GameControls();
  }
}
